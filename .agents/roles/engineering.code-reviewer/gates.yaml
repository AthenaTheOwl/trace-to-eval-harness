# Gates the engineering.code-reviewer run must pass before completion. Mirrors
# the required_gates field in role.yaml.

required_gates:
  - id: spec_check
    script: scripts/spec_check.py
    blocking: true
