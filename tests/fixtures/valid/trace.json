{
  "trace_id": "valid_trace",
  "input": "Check whether the draft report exists.",
  "output": "The draft report exists.",
  "tool_calls": [
    {
      "name": "read_file",
      "arguments": {
        "path": "reports/draft.md"
      }
    }
  ],
  "expected_behavior": {
    "required_text": ["draft report"],
    "allowed_tools": ["read_file"]
  },
  "failure_tags": ["tool_policy"]
}
