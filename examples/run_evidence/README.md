# examples/run_evidence

This directory holds canonical example packets produced from real CDCP
event logs. Each `*.packet.json` validates against
[`schemas/run-evidence.schema.json`](../../schemas/run-evidence.schema.json).

Packets in this directory follow the v2.1 packet shape
([`DEC-TTE-009`](../../decisions/DEC-TTE-009-packet-generator-resolves-repo-uris.md)
amends
[`DEC-TTE-008`](../../decisions/DEC-TTE-008-run-evidence-packet-preserves-producer-identity.md)):
every packet preserves `producer_run_id` from the producer's Run
record, carries `run_record_ref`/`run_record_hash` and
`event_log_ref`/`event_log_hash` for provenance, and clones the three
replay-equivalence fields (`prompt_snapshot_hash`,
`tool_schemas_snapshot_hash`, `sandbox_image_ref`) plus
`artifact_refs`/`artifact_hashes` from the Run record when present.
Since v2.1 (Round 6 Phase 3), ref fields use portable
`repo://<repo>@<sha>/<path>` and `artifact://<repo>/<id>` URIs per
[`DEC-CDCP-014`](https://github.com/AthenaTheOwl/athena-site/blob/main/decisions/DEC-CDCP-014-portable-ref-uris.md);
legacy local paths remain accepted for interop.

## Anchors

- Source-of-truth Run schema: amended in
  [`DEC-CDCP-011`](https://github.com/AthenaTheOwl/athena-site/blob/main/decisions/DEC-CDCP-011-run-records-carry-replay-equivalence-evidence.md)
  on athena-site; carries the six replay-equivalence fields the packets
  exercise.
- Cross-repo URI grammar: fixed in
  [`DEC-CDCP-014`](https://github.com/AthenaTheOwl/athena-site/blob/main/decisions/DEC-CDCP-014-portable-ref-uris.md)
  on athena-site. The consumer side of that contract is implemented
  here in `trace_to_eval/uri.py` and exercised by every packet below.
- Review-boundary packet contract: fixed in
  [`DEC-TTE-007`](../../decisions/DEC-TTE-007-run-evidence-packet-as-review-boundary.md),
  amended by
  [`DEC-TTE-008`](../../decisions/DEC-TTE-008-run-evidence-packet-preserves-producer-identity.md)
  (v2 producer identity), and amended by
  [`DEC-TTE-009`](../../decisions/DEC-TTE-009-packet-generator-resolves-repo-uris.md)
  (v2.1 URI resolution) in this repo. The `evidence from-cdcp-events`
  CLI reads any CDCP event log + its sibling Run record and produces a
  packet that downstream review surfaces can consume without knowing
  how the producer is wired.

## Portfolio coverage

This directory demonstrates the bridge across the full portfolio:
four product repos that each adopted the URI grammar in Round 6
Phase 2, two emitter stacks (Python and TypeScript), runs that span
LLM-driven and deterministic pipelines, and ledgers ranging from
five to fifteen events. Every packet was generated by the same
consumer CLI against an event log written by an emitter that landed
through a producer-side DEC.

| Packet | Source repo | Run id | Generated | What the Run captured | Producer DEC |
|---|---|---|---|---|---|
| [`run-7b662d3f68b1.packet.json`](./run-7b662d3f68b1.packet.json) | procurement-negotiation-lab | `run-7b662d3f68b1` | 2026-05-29 | Factory pipeline run (eight-event ledger; Python emitter) | [`DEC-FACTORY-008`](https://github.com/AthenaTheOwl/procurement-negotiation-lab/blob/main/decisions/DEC-FACTORY-008-factory-emits-portable-uri-refs.md) |
| [`run-643dff8f3b9c.packet.json`](./run-643dff8f3b9c.packet.json) | supplier-risk-rag-agent | `run-643dff8f3b9c` | 2026-05-29 | Eval suite run with refusal-precision gate (five-event ledger; Python emitter) | [`DEC-EVL-007`](https://github.com/AthenaTheOwl/supplier-risk-rag-agent/blob/main/decisions/DEC-EVL-007-eval-runner-emits-portable-uri-refs.md) |
| [`run-d74d787e6756.packet.json`](./run-d74d787e6756.packet.json) | ai-field-brief | `run-d74d787e6756` | 2026-05-29 | Brief publish (W20 backfill; fifteen-event ledger; Python emitter) | [`DEC-PUB-006`](https://github.com/AthenaTheOwl/ai-field-brief/blob/main/decisions/DEC-PUB-006-brief-emits-portable-uri-refs.md) |
| [`run-1f1fc1f3d36d.packet.json`](./run-1f1fc1f3d36d.packet.json) | ai-field-brief | `run-1f1fc1f3d36d` | 2026-05-29 | Brief publish (W21 backfill; fifteen-event ledger; Python emitter) | [`DEC-PUB-006`](https://github.com/AthenaTheOwl/ai-field-brief/blob/main/decisions/DEC-PUB-006-brief-emits-portable-uri-refs.md) |
| [`run-36e307499472.packet.json`](./run-36e307499472.packet.json) | ai-field-brief | `run-36e307499472` | 2026-05-29 | Brief publish (W22 backfill; fifteen-event ledger; Python emitter) | [`DEC-PUB-006`](https://github.com/AthenaTheOwl/ai-field-brief/blob/main/decisions/DEC-PUB-006-brief-emits-portable-uri-refs.md) |
| [`run-0ed06c8afae6.packet.json`](./run-0ed06c8afae6.packet.json) | ai-field-brief | `run-0ed06c8afae6` | 2026-06-04 | Brief publish (W23 replay-contract-catch-up; fifteen-event ledger; Python emitter) | [`DEC-PUB-006`](https://github.com/AthenaTheOwl/ai-field-brief/blob/main/decisions/DEC-PUB-006-brief-emits-portable-uri-refs.md) |
| [`run-6a665b303138.packet.json`](./run-6a665b303138.packet.json) | chip-supply-chain-map | `run-6a665b303138` | 2026-05-29 | Watchlist data refresh export (eight-event ledger; TypeScript emitter) | [`DEC-FIN-004`](https://github.com/AthenaTheOwl/chip-supply-chain-map/blob/main/decisions/DEC-FIN-004-watchlist-export-emits-portable-uri-refs.md) |
| [`run-3d0c9124c372.packet.json`](./run-3d0c9124c372.packet.json) | procurement-negotiation-lab | `run-3d0c9124c372` | 2026-07-01 | Factory test pipeline run (testrun-iqr; thirteen-event ledger, eight gates all passed; Python emitter) | [`DEC-FACTORY-008`](https://github.com/AthenaTheOwl/procurement-negotiation-lab/blob/main/decisions/DEC-FACTORY-008-factory-emits-portable-uri-refs.md) |

Every packet now carries `repo://<repo>@<sha>/<path>` URIs in
`run_record_ref`, `event_log_ref`, `sandbox_image_ref`, and (for
producers that emit file artifacts) the entries of `artifact_refs`.
The chip-supply-chain-map packet additionally exercises the
`artifact://<repo>/<id>` form for its logical watchlist packet that
has no on-disk path.

## How to regenerate

Every packet in this directory was produced by the same two-step
recipe. The generator auto-discovers the producer Run record at
`<event-log>/../run-records/<run_id>.json`; pass `--run-record <path>`
to override the lookup. Pass `--portfolio-root <path>` (or set
`PORTFOLIO_ROOT`) to override the root used for resolving `repo://`
URIs; by default the parent of this repo is used.

```powershell
# From this repo's root.
python -m trace_to_eval evidence from-cdcp-events `
  ../procurement-negotiation-lab/ops/event-ledger/run-7b662d3f68b1.jsonl `
  --out examples/run_evidence/run-7b662d3f68b1.packet.json

python -m trace_to_eval evidence validate `
  examples/run_evidence/run-7b662d3f68b1.packet.json
```

The validate command exits 0 for every packet listed above; each
matches `run-evidence.schema.json` published at
`https://trace-to-eval.dev/schemas/v2-1/run-evidence.schema.json`.

## Why this directory matters

Without an emitter the schema fields are dead letters. Without a
consumer the packet has no review surface. The packets here are the
handshake, repeated across the portfolio:

- The source-of-truth Run schema lives in athena-site and is shared
  by every producer.
- Four product repos run their own emitter wiring on top of it,
  writing event ledgers in their own languages and pipelines.
- This repo's `evidence from-cdcp-events` CLI reads any of those
  ledgers plus the sibling Run record and writes a packet that any
  review surface can consume.

The bridge is the contract; the packets here are what it produces in
practice -- with producer identity, Run-record hashes, replay-
equivalence hashes, and now portable `repo://` URIs so any consumer
can trace a packet back to a specific run at a specific commit.
